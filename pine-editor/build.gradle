plugins {
    id "java"
}

group = "dev.prozilla"
version = "1.0.0"

repositories {
    mavenCentral()
}

dependencies {
    // Pine engine
    implementation project(rootProject.name == "pine" ? ":pine-engine" : ":pine:pine-engine")
}