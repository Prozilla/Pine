package dev.prozilla.pine.core.entity;

/**
 * Represents a pool that creates and stores entity queries.
 */
public class EntityQueryPool {
	
	/*
	 * TO DO:
	 * 1. Create entity queries based on an array of component types
	 * 2. Create unique key for each query and store in pool
	 * 3. When query with same component type is fetched, return pooled query
	 * 4. If system indicates that it needs single-use query matches, always create a new query
	 * 5. Clear pool when world is destroyed
	 */
	
}
